/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Remove appDir option - it's deprecated in Next.js 14
  experimental: {
    // appDir: true
  },
}

module.exports = nextConfig

